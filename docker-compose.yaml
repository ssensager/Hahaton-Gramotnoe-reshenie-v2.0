version: "3.9"

services:
  backend:
    build: .
    container_name: rag-backend
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - ./frontend/index.html:/frontend/index.html
    environment:
      - PYTHONUNBUFFERED=1
